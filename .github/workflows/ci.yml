name: Python CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.13"
  APP_NAME: "unpdf"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        # GitHub Action best practice: caching dependencies
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest black bandit mypy
        pip install -r requirements.txt

    - name: Check code style with Black
      run: black src/ tests/ --check

    - name: Check static types with mypy
      run: mypy src/ tests/ --install-types --non-interactive

    - name: Check for security vulnerabilities with Bandit
      run: bandit -r src/ tests/ -c pyproject.toml

    - name: Test with pytest
      run: pytest

  package-and-release:
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required for creating releases
      actions: write   # Required for uploading release assets

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Increment version
      uses: saleweaver/version_increment_action@v1
      with:
        file_path: "src/unpdf/__init__.py"
        version_key: "__version__"

    - name: Create a release
      if: env.new_version != ''
      uses: softprops/action-gh-release@v3
      with:
        tag_name: v${{ env.new_version }}
        name: v${{ env.new_version }}
        generate_release_notes: true